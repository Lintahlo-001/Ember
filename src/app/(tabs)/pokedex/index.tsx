import PlaceholderScreen from '@/src/components/PlaceholderScreen';

export default function Pokedex() {
  return <PlaceholderScreen name="Pokedex" subtitle="National/Change Region toggle, search bar, grid of Pokemon (sprite, name, number)" />;
}
