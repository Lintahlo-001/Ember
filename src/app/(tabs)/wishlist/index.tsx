import PlaceholderScreen from '@/src/components/PlaceholderScreen';

export default function Wishlist() {
  return <PlaceholderScreen name="Wishlist" subtitle='"Wishlist - [count] cards" subtitle, sort/filter, column switcher, grid' />;
}
