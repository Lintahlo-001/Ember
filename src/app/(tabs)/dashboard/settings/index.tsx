import PlaceholderScreen from '@/src/components/PlaceholderScreen';

// TODO: manual "refresh cached data" control, replay onboarding hint -
// both confirmed required even though not in the original sketch.
export default function Settings() {
  return <PlaceholderScreen name="Settings" subtitle="My Account row, refresh cached data, replay onboarding hint, Log Out" />;
}
