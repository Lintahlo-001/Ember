import PlaceholderScreen from '@/src/components/PlaceholderScreen';

export default function ChangeUsername() {
  return <PlaceholderScreen name="Change Username" subtitle="new username + current password" />;
}
