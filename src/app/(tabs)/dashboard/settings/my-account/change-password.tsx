import PlaceholderScreen from '@/src/components/PlaceholderScreen';

export default function ChangePassword() {
  return <PlaceholderScreen name="Change Password" subtitle="current password, new password, confirm new password" />;
}
