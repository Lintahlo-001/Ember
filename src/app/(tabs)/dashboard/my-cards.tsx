import PlaceholderScreen from '@/src/components/PlaceholderScreen';

export default function MyCards() {
  return <PlaceholderScreen name="My Cards" subtitle="Total Cards / Total Value, search, sort/filter, 3-4-5 column switcher, grouped by series/set" />;
}
